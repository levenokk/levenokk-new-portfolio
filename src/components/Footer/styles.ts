import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  topWrapper: {
    paddingTop: 35,
    paddingBottom: 29,
    backgroundColor: '#D7FFB8',
  },
  socialsWrapper: {
    display: 'flex',
    alignItems: 'center',
  },
  socialLink: {
    marginLeft: 23,
  },
});
