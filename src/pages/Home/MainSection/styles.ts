import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
  leftWrapper: {
    maxWidth: 580,
  },
  wrapper: {
    paddingTop: 317,
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  buttonsWrapper: {
    display: 'flex',
    maxWidth: 460,
  },
  rightWrapper: {
    flexGrow: .8,
  },
});
